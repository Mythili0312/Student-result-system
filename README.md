# Student-result-system
I built a Student Result System using HTML, CSS, JavaScript, and Flask. The frontend collects student marks and sends them to the backend using fetch API. The backend processes the data, calculates total, average, and grade, and sends the result back as JSON.
